[package]
name = "theseus-store"
description = "The durable keel: append-only WAL with checksummed atomic frames and torn-tail recovery, a rebuildable index behind a trait (redb or fjall), checkpoints, and the Store contract the kernel writes through."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
anyhow.workspace = true
thiserror.workspace = true
serde.workspace = true
serde_json.workspace = true
tracing.workspace = true
crc32fast = "1"
redb.workspace = true
fjall = "3"

[dev-dependencies]
tempfile = "3"
rand = "0.9"
