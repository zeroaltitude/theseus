[package]
name = "theseus-kernel"
description = "The durable kernel (M2): executions, actions with correlation ids and retry classes, the Completion envelope and spool, budgets as hard limits, the admission scheduler, the deterministic gate, the reconciler, and the five-step startup. Synchronous and deterministic under an injected clock so the simulator can drive it."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
theseus-store.workspace = true
anyhow.workspace = true
thiserror.workspace = true
serde.workspace = true
serde_json.workspace = true
tracing.workspace = true
uuid.workspace = true
sha2 = "0.10"
hex = "0.4"
libc = "0.2"

[dev-dependencies]
tempfile = "3"
