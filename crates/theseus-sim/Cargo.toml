[package]
name = "theseus-sim"
description = "The simulator's first form: a crash-and-recover harness for the store (real SIGKILL against a worker process, torn-tail injection, full-disk), plus the redb-vs-fjall benchmark."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[[bin]]
name = "theseus-sim"
path = "src/main.rs"

[dependencies]
theseus-store.workspace = true
anyhow.workspace = true
clap.workspace = true
serde.workspace = true
serde_json.workspace = true
rand = "0.9"
tempfile = "3"
crc32fast = "1"
